apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - auth
  - core
  - task
  - web
  - mysql
  - mongo
  - postgres

labels:
  - pairs:
      app.kubernetes.io/managed-by: kustomize
      app.kubernetes.io/part-of: todoapp
    includeSelectors: true

commonAnnotations:
  contact: dalthonmh@gmail.com
