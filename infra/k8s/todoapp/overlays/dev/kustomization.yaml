apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

labels:
  - includeSelectors: true
    pairs:
      env: dev

images:
  - name: dalthonmh/todoapp-web
    newTag: "stage"

resources:
  - namespace.yaml
  - ../../base/
